/* eslint-disable no-unused-vars */
const api = require("express")();
const {
	EXPRESSURL,
	EXPRESSPORT,
	DBURL,
	DBPORT,
	DBNAME,
	DBUSERNAME,
	DBPASSWORD
} = require("./config.dev.json");

